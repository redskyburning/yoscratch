'use strict';

/**
 * @ngdoc service
 * @name yoscratchApp.rangyService
 * @description
 * # rangyService
 * Service in the yoscratchApp.
 */
angular.module('yoscratchApp')
    .service('rangyService',[function rangyService(rangy) {
        console.log(rangy);

        this.selectEventListener = function(){

        };
        
    }]);
